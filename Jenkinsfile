pipeline {
    agent { dockerfile true }
    stages {
        stage('Build') {
            steps {
                echo 'successfully build !!'
            }
        stage('Test') {
            steps {
                echo 'Test Passed !!'
            }
        }
    }
}
